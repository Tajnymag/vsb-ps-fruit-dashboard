{
  "license": "MIT",
  "private": true,
  "packageManager": "yarn@3.1.1",
  "scripts": {
    "build": "yarn workspaces foreach -pt run build",
    "heroku-postbuild": "yarn workspace fruit-dashboard-server run prisma generate",
    "start": "yarn workspace fruit-dashboard-server start"
  },
  "workspaces": [
    "packages/*"
  ]
}
